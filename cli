# CLI 명령어 가이드

이 프로젝트는 **Bun**을 기본 런타임으로 사용합니다.

---

## 📦 패키지 관리

```bash
# 의존성 설치
bun install

# 새 패키지 추가
bun add <패키지명>

# 개발 의존성 추가
bun add -d <패키지명>

# 패키지 제거
bun remove <패키지명>
```

---

## 🚀 개발 서버

```bash
# Vite 개발 서버 실행 (프론트엔드 HMR)
bun run dev

# Bun 서버 실행 (핫 리로드)
bun run serve
# 또는 직접 실행
bun --hot ./index.ts
```

---

## 🏗️ 빌드

```bash
# 프로덕션 빌드
bun run build

# 빌드 결과물 미리보기
bun run preview
```

---

## 📚 Storybook

```bash
# Storybook 개발 서버 (포트 6006)
bun run storybook

# Storybook 정적 빌드
bun run build-storybook
```

---

## 🧪 테스트

```bash
# 테스트 실행
bun test

# 특정 파일 테스트
bun test <파일경로>

# 워치 모드로 테스트
bun test --watch
```

---

## 🛠️ 유틸리티

```bash
# TypeScript 파일 직접 실행
bun <파일.ts>

# 타입 체크 (tsc 사용)
bunx tsc --noEmit

# 패키지 버전 업데이트 확인
bun outdated
```

---

## 📋 스크립트 요약

| 명령어 | 설명 |
|--------|------|
| `bun run dev` | Vite 개발 서버 실행 |
| `bun run serve` | Bun 서버 (핫 리로드) |
| `bun run build` | 프로덕션 빌드 |
| `bun run preview` | 빌드 결과물 미리보기 |
| `bun run storybook` | Storybook 실행 |
| `bun run build-storybook` | Storybook 빌드 |

---

## ⚠️ 주의사항

- `npm`, `yarn`, `pnpm` 대신 항상 **Bun**을 사용하세요.
- `.env` 파일은 Bun이 자동으로 로드합니다 (dotenv 불필요).
- 자세한 Bun API 문서는 `node_modules/bun-types/docs/` 참고.

